<%= turbo_stream_from(:chatbox_channel) %>

<div class="chat-messages" id="chatbox-user-<%= current_user.id %>">
  <% current_user.assistant_messages.daily.order(:id).each do |message| %>
    <%= render 'assistant_messages/request', message: message %>
  <% end %>
</div>
